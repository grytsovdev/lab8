import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.lab8',
  appName: 'lab8',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
